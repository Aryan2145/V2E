/**
 * The meta-permission leaf that gates "manage access rights". Holding the admin
 * flag (is_admin) grants it; the full permission tree lives in permission-registry.ts.
 */
export const ACCESS_RIGHTS_RESOURCE = 'access_rights';
